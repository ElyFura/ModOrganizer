<Window x:Class="ModOrganizer.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="480" Width="720"
        Background="{DynamicResource Bg0}" Foreground="{DynamicResource Text}"
        FontFamily="Segoe UI Variable, Segoe UI" FontSize="13"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="ClearType">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource Bg1}" Padding="24,18"
                BorderBrush="{StaticResource Border1}" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="Settings" Style="{StaticResource H1}" />
                <TextBlock Text="Mod roots that the scanner will index" Style="{StaticResource Caption}" Margin="0,3,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Padding="24,18" Background="{StaticResource Bg0}">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top" Text="MOD ROOTS" Style="{StaticResource Label}" />
                <DataGrid x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="200" />
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" />
                        <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" Width="80" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource Bg1}" Padding="24,14"
                BorderBrush="{StaticResource Border1}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add root…" Click="AddRoot_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" />
                <Button Content="Close" Click="Close_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
